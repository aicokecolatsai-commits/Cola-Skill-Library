# Workspace 安全規則

## 禁止操作
1. **刪除**：不要隨意刪除 workspace 檔案；需要清理時優先移到 `.trash/`
2. **移動**：移動檔案前需確認目的地正確
3. **安裝**：安裝工具、下載套件需先確認來源
4. **網路存取**：注意 API Key 等敏感資訊不要外洩
5. **Windows PowerShell**：使用 `npx.cmd` 而非 `npx.ps1`
